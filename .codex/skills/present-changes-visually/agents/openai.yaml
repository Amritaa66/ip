interface:
  display_name: "Present Changes Visually"
  short_description: "Create a split-view HTML diff for project changes"
  default_prompt: "Use $present-changes-visually to create a visual diff for the current changes."
